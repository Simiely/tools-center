{
  "name": "tools-center",
  "version": "0.10.0",
  "description": "Tools Center V2:统一运行时 + 可插拔能力模块 + 声明式工具插件",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node server.mjs 8080",
    "check": "node --check server.mjs && node --check lib/core/*.js && node --check lib/capabilities/*.js",
    "test": "node --test tests/*.test.mjs"
  }
}